<div class="w-full">
  <% if notice.present? %>
    <p class="py-2 px-3 bg-green-50 mb-5 text-green-500 font-medium rounded-lg inline-block" id="notice"><%= notice %></p>
  <% end %>


<div class="columns">

  <!-- Feed -->
  <div class="column is-8 mt-10">
    <!-- feed course -->
    <%= render @bacs %>
   
  </div>
</div>
</div>
